<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pritaranyu.plsystem.mapper.ImportexcelMapper">
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO test (invigilateId,examDate,session,week,dayOfWeek,startTime,endTime,campus,building,room,seatCount,studentCount,courseCode,courseName,department,teacherName,mainInvigilator,deputyInvigilator,
        mainInvigilatorCount,deputyInvigilatorCount,status) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.invigilateId},#{item.examDate},#{item.session},#{item.week},#{item.dayOfWeek},#{item.startTime},#{item.endTime},#{item.campus},#{item.building},#{item.room},#{item.seatCount},
            #{item.studentCount},#{item.courseCode},#{item.courseName},#{item.department},#{item.teacherName},#{item.mainInvigilator},#{item.deputyInvigilator},#{item.mainInvigilatorCount},#{item.deputyInvigilatorCount},#{item.status})
        </foreach>
    </insert>
</mapper>